import { LocationWatch } from './LocationWatch';

export class LocationList {
  LocationList: LocationWatch[];
  constructor(LocationList: LocationWatch[]) {
    this.LocationList = LocationList;
  }
}
