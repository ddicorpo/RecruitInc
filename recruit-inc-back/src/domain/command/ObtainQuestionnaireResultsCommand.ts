import { AbstractCommand } from './AbstractCommand';
import { RequestContext } from './RequestContext';

export class ObrtainQuestionaireResultsCommand extends AbstractCommand {
  constructor(applicationContext?: RequestContext) {
    super();
  }

  // public get questionnaireResults(page: number, filters: string[]): Promise<any> {
  //
  //
  //
  // }
}
